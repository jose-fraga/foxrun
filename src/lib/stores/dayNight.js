// Shared day/night cycle state
// sunFactor: 1 = full sunset, 0 = full night
// cycleStartTime: server timestamp (ms) for synced day/night
export const dayNight = {
  sunFactor: 1,
  cycleStartTime: 0,
}
